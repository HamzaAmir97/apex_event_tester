# Default backend the tester talks to (the "Production" option in the header
# dropdown). Used when the app is published online, so it works out of the box.
# You can still switch to Local (localhost:8000) or a custom URL from the header;
# the choice is remembered in the browser.
VITE_API_BASE_URL=https://csms-apex.brackets-tech.com

# API key for authenticating with public event endpoints (required).
# Create one from the admin panel: Settings → API Keys.
VITE_EVENT_API_KEY=evk_ANggHfqpu3qppPRdKzDNDhz4SjV57Q9S8tmcEXqyJOS0aVHp
